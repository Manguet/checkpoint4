{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
<div class="container-contact">
    <div class="container">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn main-btn">Envoyer</button>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}